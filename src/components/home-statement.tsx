import { MktSection } from "@/components/mkt-section";

export function HomeStatement() {
  return (
    <MktSection tone="inset" className="mkt-statement">
      <blockquote className="mkt-statement-quote">
        <p>
          The ring is the most valuable moment in a trade shop. Orvius makes sure it never dies in
          voicemail — and every qualified caller lands on your board, not a competitor&apos;s schedule.
        </p>
      </blockquote>
    </MktSection>
  );
}
