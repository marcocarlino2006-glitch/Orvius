# Orvius standings — have vs need (honest)

**Date:** 2026-09-16  
**Standard:** `docs/MULTI-B-STRICT.md`  
**Verdict:** Product craft is largely at institutional / beyond-bar **code** floor. Live multi-b reality is **not**. Cash, distribution, legal, and re-verified live wedge are the gap. More features will not close it.

```text
WHERE WE ARE          WHERE MULTI-B IS
─────────────────     ─────────────────────────────
Code laws green   →   Live shops paying + proving
Wedge built       →   Wedge undefeated on prod, recertified
Economics UI      →   Weekly proof ritual running
Connect coded     →   Real card $ in shop banks
Pipeline code     →   10 paying/proving partners
$0 ARR            →   First $ → retention → density
```

---

## Scorecard (this analysis)

| Layer | Have | Need | Gap |
|-------|------|------|-----|
| **A. Code laws / craft** | `beyond:check` **17/17** | Stay green forever | Small — keep enforcing |
| **B. Automated live gates** | Red in this agent (no `node_modules` / no app / no prod DB) | `test:trust` + `wedge:ready` + `standard:check` green on prod | **Env + live verify** |
| **C. SaaS money** | Paywall code done | Stripe keys + first Checkout $ | **FOUNDER — $0 ARR** |
| **D. Shop money rail** | Connect + estimate/deposit charge paths **in code** | Shop onboarded, charges+payouts enabled, real card $ | **FOUNDER + first shop** |
| **E. Wedge live** | Product + prior cert stamps in failure log | Re-verify 8/8 + 5 cell on prod now | **FOUNDER re-cert** |
| **F. Distribution** | Admin pipeline + 20-touch cadence code | Real emails, real touches, 10 partners | **FOUNDER — seeds only** |
| **G. Legal** | Placeholder language | Counsel formation state | **FOUNDER + counsel** |
| **H. Proof** | Summit self-reference possible | External named proof | **FOUNDER** |
| **I. OS monopoly** | Demand capture + taxonomy live in writers | Multi-shop compounding data | Needs paying density |
| **J. Polish debt** | Night plane densified; Settings blank-save closed; billing honesty when checkout dark | Keep era stacking dead; founder live gates | **Founder C–H** |

**Bottom line:** We are a **strong product with multi-b discipline encoded**, standing at roughly **code-ready / cash-zero / distribution-zero**. Multi-b standard requires closing layers C–H in order — not building more UI.

---

## 1. What we HAVE (earned in product)

### Laws & craft (green)
- Beyond-bar doctrine + ten laws enforced (`npm run beyond:check` 17/17)
- Prove-before-confirm on overflow
- Webhook alert drains + SMS→email failover path
- Honest money labeling (pipeline ≠ collected)
- Hard paywall: `pilotEndsAt`, 402 `billing_required`, `BillingLockScreen`
- Demand capture on inbound call/SMS + job path
- ApproveQueue + audit trail for high-risk actions
- Night-shift OS presence + live-line hero
- No vanity-green CI skips
- Taxonomy append-only + monopoly sequence documented

### Wedge product (built)
- Dedicated line model, exclusive number resolution
- Capture → Record → Command → Proof surfaces
- Owner alerts, Attention queue, customer confirm loop
- `/demo` honesty gates, overflow forward truth
- Founder cert UI persisted via `founderCertJson`
- War-repair / shared-line repair scripts

### Money product (built further than old roadmap claimed)
- SaaS Checkout / portal / webhook routes
- **Stripe Connect Express** (`stripe-connect.ts`) — direct charges, application fee
- Estimate card pay (`estimate-pay.ts`) + booking deposits (`booking-deposit.ts`)
- Estimate / Invoice / Payment models
- Connect onboarding API + payouts panel
- Economics panel, weekly proof API, data export

### Ops / trust product (built)
- Cron auth fail-closed patterns
- Tenant isolation expectations in standard-check
- Sentry wiring present (DSN still optional/founder)
- Institutional SLAs in owner UI

### Docs / gates
- `MULTI-B-STRICT`, `BEYOND-BAR`, battles, Manus bar, wedge mastery, failure log

---

## 2. What LIVE REALITY still is (not multi-b yet)

| Item | Live status | Evidence |
|------|-------------|----------|
| Stripe SaaS keys | **Missing** in this env; billing:check **5 blockers** | `billing:check` |
| ARR | **$0** | Roadmap + billing red |
| Formation | **null** | `company.formationStateConfirmed` |
| Resend failover | **Founder paste** still open | Failure log partial |
| Outreach | **Seed prospects** — replace before live | Failure log |
| External proof | **None** | Strict list |
| Connect live shop | Code ready; **no claim of live payouts** until a shop is onboarded + charged | Connect `canAcceptPayments` requires live account |
| Agent trust/wedge | Cannot certify here until `npm install` + prod DB + running app | Missing `@prisma/client` |
| CSS landfill | **DONE ceiling** — `globals.css` ~7.6k (&lt;8k); product densify lives in `dashboard.css` | `globals.css` + `dashboard.css` |
| Public post bar | **DO NOT POST** until bulletproof full green | Manus bar |

### Failure-log nuance (don’t lie either way)
Earlier stamps (2026-09-05) say Summit wedge 8/8, founder cert 5/5, baselines + weekly proof set.  
Those are **historical stamps**, not a substitute for **re-verify now** before public claims or outreach volume. Multi-b standard = current truth, not last month’s checkbox.

---

## 3. Gap map vs MULTI-B-STRICT

| Strict section | Standing | What’s left |
|----------------|----------|-------------|
| **0. Laws** | ✅ Code | Maintain; never waive |
| **1. Wedge** | 🟡 Built + prior stamp | Re-cert on prod cell; keep failure log clear |
| **2. Reliability** | 🟡 Mostly code | Resend live; Sentry DSN; live standard:check on prod |
| **3. SaaS money** | 🔴 Code only | Keys → setup → **first $** |
| **3. Shop money rail** | 🟡 Code advanced | First shop Connect onboard + real card settlement |
| **4. Outcome honesty** | ✅ Surfaces | Ritual running weekly on live shops |
| **5. Craft** | ✅ Strong | CSS cut (B6); stay brand-swap clean |
| **6. Monopoly** | 🟡 Capture live | Needs many shops before benchmarks |
| **7. Distribution** | 🔴 Machine empty | Real list → 20/day → 10 partners |
| **8. Legal** | 🔴 | Counsel formation |
| **9. OS expansion** | ⏸ Hold | No plugs/marketplace until money + density |
| **10. Cadence** | 🟡 Defined | Founder must run daily/weekly rituals |
| **11. Hard bans** | ✅ Documented | Enforce — no premature claims |

---

## 4. Distance to multi-b (four pass layers)

| Layer | Status | Blocker type |
|-------|--------|--------------|
| **Floor (code)** | Mostly **PASS** (`beyond:check`) | Keep green; install deps in every env that claims CI |
| **One shop** | **NEAR** if prior Summit stamps still true — must re-verify | Founder phone + prod wedge |
| **Ten shops** | **FAIL** | First $ · external proof · real outreach · 10 partners · formation |
| **Category OS** | **FAIL** | Connect live at volume · density · then plugs |

We are **between Floor and One-shop**. We are **not** at Ten-shops or Category OS.

---

## 4b. Look & operate — is it multi-b level?

**Short answer: not yet.** Intent and some craft are strong. The surface still reads
as a serious early product, not as Toast / Stripe / Linear inevitability.

### Look (presence / brand / UI)

| Bar | Multi-b standard | Orvius today | Grade |
|-----|------------------|--------------|-------|
| Brand-swap test | Unmistakable without the nav | Night-shift claim + live line + **Orvius** brand in hero; Cursor mega-nav removed | **B+** |
| First viewport budget | Brand + one claim + one CTA group + one dominant artifact | Hero only — brand + claim + live line + CTA; eyebrow removed | **A** |
| Visual system | One era, one letterset, ruthless cohesion | Archivo + Plex Mono. `globals.css` ~7.45k; cursor/marketing-page era classes dropped from public shell | **B+** |
| Real atmosphere | Product/place/context as the visual idea | Full-bleed night hero: copper bay bloom, board grid, horizon + live console | **A−** |
| Confidence | Quiet authority (Anthropic/Stripe) | Direct claim + live line; less funnel noise | **B+** |
| Product shell | Calm utility (Claude/Linear) | OsShell + Command + Ask dock + founder next-gate | **A−** |

**Look verdict:** Night OS presence with intentional hero motion (bay breathe, liveline, pulse). Density still needs live shops; keep `globals.css` under 8k.

### Operate (how the company and product run)

| Bar | Multi-b standard | Orvius today | Grade |
|-----|------------------|--------------|-------|
| Owner at 2am | Alert + act without hunting | Attention queue + prove ritual designed for this | **B+** (code) / **?** (live) |
| Fail closed | Money/auth/cron never soft | Paywall, 401, cron secret, confirm-after-prove | **A−** (code) |
| Honest money | Pipeline ≠ collected; no fake checkout | Enforced in UI + checks | **A−** (code) |
| Daily cadence | Same rituals every day | Shop operate pulse + `/admin/daily` + Command board language | **A−** tool / habit still founder |
| Support | Stated SLA matches staffing | Best-effort honesty — correct | **B** |
| Public claims | Only what works in prod | Manus / bulletproof gates | **A−** (discipline) / red until cash live |
| Shipping craft | One system, no landfill | `globals.css` under 8k; product densify in dashboard sheet | **B+** |

**Operate verdict:** Tools now force the next gate into view. Live multi-b operate still
requires founder to close cash, cert, outreach, and formation — UI cannot invent those.

### Combined scorecard

```text
LOOK     ████████████  ~90% craft (B6 line ceiling clear; density still live shops)
OPERATE  ████████████  ~94% craft  |  residual ~6% = you running it live
MULTI-B  founder gates still yours (Stripe/cert/etc.)
```

**Operate craft ceiling (code):** shop pulse with inline proof + test alert, every
board kind mapped to a one-tap strategy, mobile sticky pulse + 44px actions.

**Residual ~6% (not inventable in code):** daily habit — you actually clear the
pulse, stamp proof, and keep the live line green. That last slice is operation,
not UI.

---

## 5. Ordered work (no corners, no skipping)

**Cockpit:** `/admin/daily` (Master all) · **CLI:** `npm run master:all`

### Founder-critical (blocks multi-b) — do in this order
1. Paste Stripe keys → `npm run stripe:setup` → webhook → **first paid Checkout** ($0 → ARR)
2. Paste `RESEND_API_KEY` on Vercel (SMS→email failover live)
3. Re-run live cell cert 5/5 + `wedge:ready` 8/8 on **prod** (historical stamps do not count)
4. Confirm Summit weekly proof still current (or re-stamp on Command)
5. Onboard first shop to **Connect** in Billing → payouts → take one real card $ to their bank
6. Replace seed prospects → run `/admin/daily` for real outreach
7. Counsel: formation state → set `formationStateConfirmed`
8. Land one **external** named proof (not Summit self-reference)

Only after 1–8: density (10 proving shops), then deepen rings. More Settings UI will not skip this list.

### Agent/code (this PR closes Settings honesty debt)
9. Settings night capture + cert/Manus honesty + Billing money copy — **shipped on this branch**
10. Keep `beyond:check` / `master:class` / `multi-b:check` green — never vanity-green
11. Keep `globals.css` under 8k — product densify belongs in `dashboard.css`
12. Only after founder 1–8: money-rail ops polish when a paying shop asks

---

## 6. What NOT to do (corners that kill the path)

- Build more homepage / rings / integrations to feel progress
- Post publicly with red bulletproof / missing Stripe
- Call Summit a customer case study
- Claim Connect payouts or Jobber sync before live
- Skip to marketplace without metro density
- Treat historical cert stamps as forever-green

---

## Commands to re-measure

```bash
npm ci
npm run db:generate
npm run beyond:check
npm run master:all
npm run multi-b:check
npm run billing:check
npm run wedge:ready          # needs prod DB + live line
npm run bulletproof          # full bar before any post
```

Open every morning: **/admin/daily** (Master all scorecard)

---

*This file is the standing analysis. The requirement list is `docs/MULTI-B-STRICT.md`. The ceiling doctrine is `docs/BEYOND-BAR.md`.*
