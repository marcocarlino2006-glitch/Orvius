{
  "framework": "nextjs",
  "buildCommand": "npm run db:generate && npm run db:migrate:turso && npm run build",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/notifications",
      "schedule": "0 9 * * *"
    }
  ]
}
